import React, {useEffect} from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const DealDetails = () => {
  return (
    <View>
      <Text styles={{fontSize: 20, fontWeight: '600'}}>Deals Details</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default DealDetails;
